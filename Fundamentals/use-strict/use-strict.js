// ⚠️ What Strict Mode Does:
// Prevents using undeclared variables.

// Disallows deleting variables/functions.

// Catches silent errors (like assigning to read-only properties).

// Disallows duplicate parameter names.

// Makes this in functions undefined (not window) if not in an object.

// 📄 Suggested File Name with Strict Mode: