let a = {
  x: 10,
  getX() {
    return this.x;
  }
};

let b = Object.create(a);
b.x = 20;

let c = Object.create(b);
c.x = 30;

console.log(c.getX()); // ❓ What will be the output?



// ✅ ব্যাখ্যা (বাংলা ফন্টে):
// a হল একটি অবজেক্ট যেটার মধ্যে x = 10 এবং একটি মেথড আছে: getX() যা this.x রিটার্ন করে।
// b = Object.create(a)
// এখন b অবজেক্টের প্রোটোটাইপ হল a।
// তার মানে, b.getX() কল করলে, জাভাস্ক্রিপ্ট a থেকে মেথডটা পাবে।
// c = Object.create(b)
// এখন c এর প্রোটোটাইপ হল b
// অর্থাৎ c → b → a এই প্রোটোটাইপ চেইন তৈরি হল।
// c.getX() কল করলে কী হবে?
// getX() মেথড c তে নাই → b তেও নাই → a তে পাওয়া যাবে।
// কিন্তু getX() এর ভিতরে this.x বলা আছে।
// 👉 এখন গুরুত্বপূর্ণ বিষয় হলো:
// যখন c.getX() কল করা হয়, তখন this রেফার করে c অবজেক্টকে, কারণ getX() কে c কল করেছে।
// আর c.x = 30 আছে, তাই this.x = 30 রিটার্ন করবে।

